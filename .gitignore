# Go 后端构建产物
backend/server.exe
backend/*.exe
backend/*.test

# Node/Vue 前端
frontend/node_modules/
frontend/dist/
frontend/.npm-cache/

# 本地环境变量（含登录账号密码），不要提交
.env

# 手动拉取的数据库价格缓存（会频繁变化，不入库）
data/db_price_cache.json

# 临时/生成文件
*.log
